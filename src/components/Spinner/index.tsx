import React, { FC } from "react";
import * as SC from "./styles";

const Spinner: FC = () => {
  return (
    <SC.Spinner>
      <div></div>
      <div></div>
    </SC.Spinner>
  );
};

export default Spinner;
